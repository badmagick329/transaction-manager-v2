# TCP port exposed on the Linux host for LAN and Tailscale access.
HOST_PORT=4121

# App port inside the container.
PORT=3000

# Persistent runtime paths inside the mounted /app/data directory.
DATA_DIR=/app/data
DATABASE_PATH=/app/data/app.db
IMPORTS_DIR=/app/data/imports
